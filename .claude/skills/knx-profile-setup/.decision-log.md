---
phase: built
classification: simple-workflow
last_touched: 2026-05-31
---

# Decision Log: knx-profile-setup

## 2026-05-31 - Initial Build

Built `knx-profile-setup` as a module-based Simple Workflow under module code `knx`.

Decisions:

- Chose Simple Workflow because the skill has a clear profile/scaffold output and does not need carved references yet.
- Kept the first version fixed with no `customize.toml`; module setup/config will be handled later when the installable module is scaffolded.
- Did not add scripts in the first pass. The workflow can create markdown/profile artifacts through normal file edits; deterministic detection scripts can be added later if local resource profiling becomes too detailed for prompt execution.
- Made setup conservative by default: no external providers, no source mutation, no sends, no credentials, no customer/production access, and no ambiguous storage writes when setup is incomplete.
- Treated OneDrive, GitHub, external LLM providers, host GPU access, and Microsoft 365 as optional choices rather than assumptions.

Source context:

- `skills/reports/kendallai-vnext-module-plan.md`
- `_bmad-output/specs/spec-kendallai-vnext/SPEC.md`

Next recommended build: `knx-execution-policy`.

Lint note:

- `scan-scripts.py` passed.
- `scan-path-standards.py` reported `.decision-log.md` at skill root as a high-severity structure finding.
- The workflow-builder build process explicitly requires `.decision-log.md` at the skill root as a peer of `SKILL.md`, so the file was not moved to `references/`. This is recorded as a scanner/instruction conflict rather than a target-skill defect.
