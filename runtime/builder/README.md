# Builder Support

This folder is reserved for builder-side assets that feed runtime packaging, such as:

- generated module manifests
- packaging helpers
- normalized design artifacts
- scenario and intake normalization outputs

The shipped runtime should not expose builder internals to end users.
