# Runtime Contracts

This folder contains the shared machine-readable contracts used by the builder side and the runtime.

The first required contract is the module contract schema. It defines installation, trust, permission, memory, privacy, and observability rules for installable modules.
