# Runtime Modules

Runtime users install clean capabilities as modules. Internally, a module may contain multiple specialized agents, but the runtime treats the module as the product contract boundary for installation, permissions, trust posture, updates, and observability.

Release 1 modules:

- `outlook/`
- `scheduling/`
- `tasks/`
